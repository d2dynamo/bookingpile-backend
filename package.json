{
  "name": "bookingpile-back",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun ./src/index.ts",
    "test": "bun --env-file=./.env.test test",
    "build": "bun build ./src/index.tsx --outdir ./build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0",
    "drizzle-kit": "^0.30.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.4",
    "express": "^4.21.2"
  }
}
